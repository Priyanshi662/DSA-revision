#include <iostream>
using namespace std;

void pushk(int arr[],int x,int top[],int stkno)
{
    if(top[stkno]!=(stkno))
    {

    }
}
int main()
{
    int n,k;
    cin>>n>>k;
    int arr[n+1];
    // 1. stack number
    // 2. push 
    // 3. pop
    int top[k+1]={-1};

}